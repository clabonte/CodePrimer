{# #}
### Business Relations

| Name | Type | Description | Relationship |
| ---- | ---- | ----------- | ------------ |
{% for relation in businessModel.relations %}
| **{{ relation.field.name }}** | {{ relation.field|type }} | {{ relation.field.description }}  | {{ relation.relationship.type }} *({{ relation.bidirectional ? 'bidirectional' : 'unidirectional' }} - {{ relation.side }})* |
{% endfor %}