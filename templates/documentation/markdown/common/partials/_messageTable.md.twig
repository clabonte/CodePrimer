### Message {{ message.id }}
{{ message.name }}: {{ message.description }}

**Data**:

| Variable | Type | BusinessModel | Field | Description | Level |
| -------- | ---- | ------------- | ----- | ----------- | ------|
{% for dataBundle in message.dataBundles %}
    {%~ for model in dataBundle.data %}
        {%~ for data in model %}
| {{ data.name }} | {{ data|type }} | {{ data|model }} | {{ data.field.name }} | {{ data.field.description }} | {{ data|details }} |
        {%~ endfor %}
    {%~ endfor %}
{% endfor %}

**Example**:
