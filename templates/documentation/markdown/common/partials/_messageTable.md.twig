### Message {{ message.id }}
{{ message.name }}: {{ message.description }}

**Data**:

| BusinessModel | Field | Type | Description | Level |
| ------------- | ----- | ---- | ----------- | ------|
{% for dataBundle in message.dataBundles %}
    {%~ for model in dataBundle.data %}
        {%~ for data in model %}
| [{{ data.businessModel|class }}](../DataModel/{{ data.businessModel|class }}.md) | {{ data.field.name }} | {{ data.field.list ? 'List of ' }}{{ data.field is entity(bundle) ? "[#{data.field.type|class}](../DataModel/#{data.field.type|class}.md)" : data.field.type }} | {{ data.field.description }} | {{ data.details|title }} |
        {%~ endfor %}
    {%~ endfor %}
{% endfor %}

**Example**:
